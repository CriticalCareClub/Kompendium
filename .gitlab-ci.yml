image: alpine

pages:
  script:
  - apk --no-cache add file git py3-pip python3-dev imagemagick zlib-dev jpeg-dev musl-dev py3-setuptools python3-dev gcc zip libxml2-dev libxslt-dev
  - convert --version
  - set LIBRARY_PATH=$LIBRARY_PATH:/lib:/usr/lib
  - pip3 install --upgrade pip
  - pip3 install Pillow
  - pip3 install sphinx sphinx-material sphinxcontrib.bibtex sphinx_git sphinxcontrib-actdiag sphinxcontrib-blockdiag sphinxcontrib-seqdiag sphinxcontrib-nwdiag
  - apk --no-cache add make
  - pwd
  - git checkout master
  - git status
  - mkdir ~/.font
  - cp Quelltext/_static/source-sans-3/TTF/* ~/.font
  - make html
  - mv Export/master/html public
  artifacts:
    paths:
    - public
  only:
  - master